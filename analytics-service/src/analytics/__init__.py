"""MariaAlpha Analytics Service.

Houses three Phase-2 analytics features:

- ``toxicity`` — flow toxicity / adverse-selection detector (issue 2.2.4)
- ``pnl`` — PnL attribution into spread / timing / impact / commissions (issue 2.2.5)
- ``axes`` — client-interest / axe-matching model (issue 2.2.6)
"""
