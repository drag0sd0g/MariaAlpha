"""MariaAlpha Analytics Service.

Houses three analytics features:

- ``toxicity`` — flow toxicity / adverse-selection detector
- ``pnl`` — PnL attribution into spread / timing / impact / commissions
- ``axes`` — client-interest / axe-matching model
"""
