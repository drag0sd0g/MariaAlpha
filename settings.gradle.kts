rootProject.name = "mariaalpha"

include(
    "proto",
    "market-data-gateway",
    "strategy-engine",
    "execution-engine",
    "order-manager",
    "post-trade",
    "api-gateway",
    "e2e-tests"
)
